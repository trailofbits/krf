proto: char __user *name, int len
parms: name, len
errors:
  - EFAULT
  - EINVAL
  - ENAMETOOLONG
  - EPERM
profiles:
  - sys
  # TODO(ww): Does it make sense to add this to the net profile?
  # - net
